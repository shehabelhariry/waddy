The following is my CV summary:
```
{{cv_text}}
```

You are required to convert this summary into a structured JSON structure that matches the schema of the following object. 
Take into consideration that only the sections matched by you should be filled, you can ignore other sections.
You should return the output JSON between <new_cv></new_cv> tags. 

This is the sample of the JSON: 
```
{{sample_json}}
```